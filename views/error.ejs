<div class="destroyed-wrap">
  <div style="font-family:var(--mono);font-size:3rem;color:var(--muted);font-weight:700"><%= code %></div>
  <h2 style="font-size:1.25rem;margin-top:.75rem"><%= title %></h2>
  <p style="color:var(--dim);margin-top:.5rem"><%= message %></p>
  <a href="/" class="btn btn-outline btn-sm" style="margin-top:1.5rem">Home</a>
</div>
